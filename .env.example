# 本機開發用範例設定；複製為 .env 後填入實際值。
# .env 僅供本機使用，切勿提交至 Git（見 docs/DEVELOPMENT_SPEC.md #12）。
# OLLAMA_API_KEY 不可出現在前端、Git、測試快照或 log。

# Ollama Cloud
OLLAMA_API_KEY=
OLLAMA_BASE_URL=https://ollama.com
OLLAMA_MODEL=gemma4:31b-cloud

# 005 RAG：legal_sources 檢索用的 embedding 模型。留空時系統會優雅降級為
# NullKnowledgeRepository（無 RAG 依據，003 既有審閱流程不受影響）。
# 已確認 Ollama Cloud 完全不提供 embedding 模型（只有對話/推理模型），
# 因此這裡必須指向「本機」執行的 Ollama（ollama serve + ollama pull），
# 與上面雲端的 OLLAMA_BASE_URL 分開設定，互不影響。
OLLAMA_EMBEDDING_MODEL=qwen3-embedding:0.6b
OLLAMA_EMBEDDING_BASE_URL=http://localhost:11434
